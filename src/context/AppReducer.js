import React from 'react'

export default AppReducer = (state, action) => {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            return state;
    }
}
